# Windows Desktop Calendar Widget v0.1 설계 명세

## 1. 프로젝트 개요

### 목적

Windows 바탕화면에 항상 표시되는 개인용 캘린더 Widget을 구축한다.

Google Calendar의 일정을 가져와 표시하되, 별도의 중계 서버 없이 사용자의 PC와 Google Calendar가 직접 통신하도록 구성한다.

### 핵심 목표

* Windows 10/11 지원
* 바탕화면 Widget 형태
* 지정 위치 및 크기 유지
* Windows 로그인 시 자동 실행
* Google Calendar 연동
* 사용자 PC 외 별도 서버 사용하지 않음
* OAuth Token 안전하게 저장
* 최소한의 Google 권한만 요청
* 가볍고 빠른 실행

### Non-Goal

초기 버전에서는 다음 기능은 제외한다.

* 모바일 앱
* 여러 사용자 간 일정 공유 기능
* 자체 Calendar Server
* Outlook / Apple Calendar 연동
* 회사 단위 계정 관리
* 복잡한 일정 편집기
* WorkerW 등 Windows Explorer 내부 Window에 직접 삽입하는 방식

---

# 2. 기술 Stack

| 영역           | 기술                                     |
| ------------ | -------------------------------------- |
| Language     | Python 3.12+                           |
| GUI          | PySide6                                |
| Calendar API | Google Calendar API                    |
| 인증           | Google OAuth 2.0 Desktop App           |
| HTTP         | google-api-python-client               |
| Local DB     | SQLite                                 |
| ORM          | 우선 사용하지 않음                             |
| Secret 저장    | Windows Credential Manager (`keyring`) |
| Windows API  | `ctypes` / `pywin32`                   |
| Packaging    | PyInstaller                            |
| 설정           | JSON                                   |
| Logging      | Python `logging`                       |

가능한 한 Python 표준 Library 및 검증된 Library만 사용한다.

---

# 3. 전체 Architecture

```text
┌─────────────────────────────────────────┐
│             Desktop Widget              │
│                                         │
│  Month View / Event View / Settings     │
│               PySide6                   │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│           Application Service           │
│                                         │
│ CalendarService                         │
│ SyncService                             │
│ SettingsService                         │
│ StartupService                          │
└──────────────┬──────────────┬───────────┘
               │              │
       ┌───────▼──────┐ ┌────▼──────────┐
       │ Google       │ │ Local Storage │
       │ Calendar API │ │               │
       │              │ │ SQLite        │
       └───────┬──────┘ │ JSON Config   │
               │        └───────────────┘
               │
        Google OAuth 2.0
               │
       ┌───────▼───────────┐
       │ Windows Credential│
       │ Manager           │
       │                   │
       │ OAuth Token 저장  │
       └───────────────────┘
```

중요한 원칙은 다음과 같다.

> Google Calendar ↔ 사용자 PC

사이에 별도의 자체 Server를 두지 않는다.

---

# 4. UI 설계

## 4.1 기본 화면

기본 형태는 월간 Calendar다.

```text
┌─────────────────────────────────────────────┐
│  ‹       September 2026        ›      ⚙    │
├──────┬──────┬──────┬──────┬──────┬─────┬───┤
│ Mon  │ Tue  │ Wed  │ Thu  │ Fri  │ Sat │Sun│
├──────┼──────┼──────┼──────┼──────┼─────┼───┤
│      │  1   │  2   │  3   │  4   │  5  │ 6 │
│      │      │      │ 발표 │      │     │   │
├──────┼──────┼──────┼──────┼──────┼─────┼───┤
│  7   │  8   │  9   │ 10   │ 11   │ 12  │13 │
│      │      │      │      │ 세미나│     │   │
└──────┴──────┴──────┴──────┴──────┴─────┴───┘
```

### Widget 특성

* Frame 없음
* Windows Taskbar에 표시하지 않음
* 항상 위(Always-on-top) 사용하지 않음
* 기본적으로 다른 Window 아래에 위치
* 위치 이동 가능
* 크기 조절 가능
* 마지막 위치/크기 기억
* 투명도 설정 가능
* 배경색 설정 가능
* Light / Dark Theme
* 날짜 Cell에 일정 최대 2~3개 표시
* 초과 일정은 `+3` 등으로 표시

---

# 5. Desktop 동작

Windows Explorer의 `WorkerW` Window 안으로 프로그램을 강제로 삽입하지 않는다.

대신 일반 Window를 Desktop Widget처럼 동작시키도록 한다.

```text
Frameless Window
        +
Tool Window
        +
Taskbar Hide
        +
HWND_BOTTOM
        +
Position Persistence
```

Windows API의 `SetWindowPos(HWND_BOTTOM)` 등을 사용하여 일반 Window보다 뒤에 위치하도록 한다.

장점:

* Windows Update 영향 감소
* Explorer 재시작 영향 감소
* 구현 단순
* Debug 용이
* OS 내부 비공식 구조 의존성 감소

---

# 6. 위치 및 크기 저장

사용자가 Widget을 이동하거나 크기를 변경하면 다음 정보를 저장한다.

```json
{
    "window": {
        "x": 1420,
        "y": 60,
        "width": 450,
        "height": 650,
        "opacity": 0.92
    }
}
```

저장 위치:

```text
%APPDATA%/DesktopCalendar/config.json
```

프로그램 재시작 시 이전 위치를 복원한다.

Multi-monitor 환경을 고려하여 Monitor가 사라진 경우에는 주 Monitor 영역으로 자동 복구한다.

---

# 7. Google Calendar 연동

## 인증

Google OAuth 2.0 Desktop Application 방식을 사용한다.

최초 실행:

```text
DesktopCalendar 실행
        ↓
Google 로그인
        ↓
Calendar 권한 승인
        ↓
OAuth Token 발급
        ↓
Windows Credential Manager 저장
        ↓
Calendar 동기화
```

### 초기 권한

v0.1에서는 **Read Only**를 권장한다.

```text
calendar.readonly
```

이유:

* 최소 권한 원칙
* 실수로 Google Calendar 데이터를 수정할 가능성 차단
* 초기 Debug 단순화

추후 일정 추가/수정 기능을 만들 때 Write 권한을 추가한다.

---

# 8. Token 보안

다음과 같은 파일은 만들지 않는다.

```text
token.json
refresh_token.txt
password.txt
```

OAuth Refresh Token은 Windows Credential Manager에 저장한다.

```text
Service:
DesktopCalendar

Credential:
google_oauth_token
```

Python에서는 다음 Library 사용을 우선 검토한다.

```text
keyring
```

따라서 Token 저장 구조는 다음과 같다.

```text
Google
  ↓
OAuth Token
  ↓
Windows Credential Manager
```

SQLite에는 Token을 저장하지 않는다.

---

# 9. Local Database

SQLite는 Google Calendar의 Cache 용도로만 사용한다.

```text
calendar.db
```

### Event

```sql
events
------------------------
id
calendar_id
google_event_id
title
start_time
end_time
all_day
location
status
updated_at
synced_at
```

Google Event의 상세 설명이나 참가자 정보 등은 실제 표시가 필요한 경우에만 저장한다.

가능하면 Google에서 받은 정보를 최소한으로 Cache한다.

---

# 10. 동기화

기본 Sync 주기:

```text
앱 실행 직후
+
5분마다
+
사용자 Refresh 버튼
```

흐름:

```text
Sync Trigger

   ↓

Google Calendar API

   ↓

현재 표시 월 기준 일정 조회

   ↓

SQLite Cache Update

   ↓

UI Refresh
```

Internet 연결 실패 시에는 마지막 Cache를 표시한다.

```text
Google 연결 실패
       ↓
Local Cache 사용
       ↓
"마지막 동기화: 17:42"
```

형태로 상태만 표시한다.

---

# 11. Windows 자동 시작

사용자가 Settings에서 다음 옵션을 활성화할 수 있다.

```text
☑ Windows 시작 시 자동 실행
```

Registry:

```text
HKCU\
Software\
Microsoft\
Windows\
CurrentVersion\
Run
```

에 현재 실행 파일을 등록한다.

관리자 권한은 요구하지 않는다.

---

# 12. Settings

초기 설정 항목은 다음 정도로 제한한다.

```text
General
 ├─ Windows 시작 시 자동 실행
 ├─ Calendar 시작 요일
 └─ Sync 주기

Appearance
 ├─ Light / Dark
 ├─ Background opacity
 ├─ Font size
 └─ Event 표시 개수

Google Calendar
 ├─ Google 계정
 ├─ 표시 Calendar 선택
 ├─ 마지막 Sync
 └─ 로그아웃
```

---

# 13. 프로그램 Directory

```text
desktop-calendar/
│
├─ src/
│   ├─ main.py
│   │
│   ├─ ui/
│   │   ├─ main_window.py
│   │   ├─ calendar_widget.py
│   │   ├─ day_cell.py
│   │   └─ settings_dialog.py
│   │
│   ├─ services/
│   │   ├─ calendar_service.py
│   │   ├─ google_auth_service.py
│   │   ├─ sync_service.py
│   │   ├─ startup_service.py
│   │   └─ settings_service.py
│   │
│   ├─ repositories/
│   │   └─ event_repository.py
│   │
│   ├─ models/
│   │   └─ event.py
│   │
│   ├─ infrastructure/
│   │   ├─ database.py
│   │   ├─ credential_store.py
│   │   └─ windows_api.py
│   │
│   └─ config/
│       └─ default_config.py
│
├─ tests/
│   ├─ unit/
│   └─ integration/
│
├─ requirements.txt
├─ README.md
└─ pyproject.toml
```

UI / Google / Windows API / Storage를 분리한다.

따라서 추후 UI를 변경하거나 Google Calendar 이외의 Calendar Provider를 추가해도 핵심 Logic을 크게 수정하지 않도록 한다.

---

# 14. 주요 Class

```text
MainWindow
 └─ 화면 및 사용자 Interaction

CalendarService
 └─ Calendar 조회 Interface

GoogleCalendarService
 └─ Google API 구현체

GoogleAuthService
 └─ OAuth 인증

SyncService
 └─ Google ↔ SQLite 동기화

EventRepository
 └─ SQLite Event 관리

CredentialStore
 └─ OAuth Token 저장/조회

StartupService
 └─ Windows 자동 시작

SettingsService
 └─ 사용자 설정 저장

WindowsDesktopService
 └─ Window 위치 / HWND_BOTTOM 처리
```

---

# 15. 오류 처리

Network 오류가 발생해도 Widget 자체가 종료되어서는 안 된다.

예:

```text
Network Error
→ Local Cache 표시

OAuth 만료
→ 재로그인 요구

Google API Error
→ 다음 Sync까지 기존 Cache 유지

SQLite Error
→ DB 재생성

잘못된 Monitor 좌표
→ Primary Monitor로 이동
```

---

# 16. Logging

기본 Log 위치:

```text
%LOCALAPPDATA%/
DesktopCalendar/
logs/
app.log
```

Log에는 다음은 기록하지 않는다.

```text
OAuth Access Token
OAuth Refresh Token
Google Password
Authorization Header
```

기록 대상은 다음 정도로 제한한다.

```text
Application Start
Application Close
Sync Start
Sync Complete
API Error
DB Error
UI Error
```

---

# 17. v0.1 구현 범위

### MUST

* [ ] Desktop Widget
* [ ] 월간 Calendar
* [ ] 현재 날짜 표시
* [ ] Window 위치 이동
* [ ] Window 크기 변경
* [ ] 위치/크기 저장
* [ ] Google OAuth 로그인
* [ ] Google Calendar 일정 조회
* [ ] SQLite Cache
* [ ] Offline Cache 표시
* [ ] Windows Credential Manager Token 저장
* [ ] 자동 Sync
* [ ] Windows 자동 실행
* [ ] System Tray
* [ ] 앱 종료 / 설정 메뉴

### SHOULD

* [ ] 투명도 설정
* [ ] Dark Mode
* [ ] Calendar별 표시 On/Off
* [ ] 일정 색상 표시
* [ ] Refresh 버튼

### v0.1 제외

* [ ] 일정 생성
* [ ] 일정 수정
* [ ] 일정 삭제
* [ ] Reminder Notification
* [ ] Outlook Calendar
* [ ] Apple Calendar
* [ ] Todo
* [ ] Weather

---

# 18. v0.2 후보

v0.1이 안정화되면 다음을 추가한다.

```text
날짜 Double Click
       ↓
Quick Event 추가

Event Click
       ↓
일정 상세

Right Click
       ↓
수정 / 삭제
```

이 단계부터 Google Calendar Write Scope를 요청한다.

---

# 19. 테스트 기준

## 기능 테스트

```text
PC 재부팅
→ Widget 자동 실행

Widget 이동
→ 재실행
→ 동일 위치 복구

Google 일정 추가
→ 최대 5분 이내 Widget 반영

인터넷 차단
→ Widget 정상 실행
→ 기존 일정 표시

OAuth Token 삭제
→ 로그인 요구

Google 로그아웃
→ Local Credential 삭제
```

## 보안 테스트

다음 위치에 OAuth Token 평문이 존재하지 않아야 한다.

```text
프로젝트 Directory
AppData
SQLite
Log
config.json
```

## 안정성 테스트

```text
Explorer 재시작
Monitor 연결/해제
절전모드 복귀
Network 연결/해제
Google API Timeout
```

에서도 프로그램이 비정상 종료되지 않아야 한다.

---

# 20. MVP 완료 조건

다음 Scenario가 정상 동작하면 v0.1을 완료한 것으로 본다.

> Windows 로그인 → Calendar Widget 자동 실행 → 이전 위치에서 표시 → Google Calendar 일정 자동 동기화 → 인터넷 연결이 없어도 기존 일정 표시 → 일반 프로그램을 실행하면 Calendar는 그 뒤에 위치 → Calendar의 OAuth Credential은 Windows Credential Manager에서만 관리

이 범위라면 별도의 Server가 필요하지 않으며, 개인용 Desktop Calendar로서는 충분히 실용적인 수준의 첫 버전이 된다.
