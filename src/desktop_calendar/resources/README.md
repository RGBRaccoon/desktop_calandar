# 배포자 Google OAuth 설정

Google Cloud에서 **데스크톱 앱** OAuth 클라이언트 JSON을 다운로드하여 이 폴더에 `oauth_client.json`으로 저장합니다.
이 파일은 Git에서 제외됩니다. 서비스 계정 키나 사용자 token.json을 넣지 마세요.
배포 spec이 등록 설정을 검증하여 단일 EXE에 포함합니다. 사용자 로그인 토큰은 포함하지 않습니다.
